#ifndef LOBBY_H
#define LOBBY_H

#include <QMainWindow>

class lobby : public QMainWindow
{
    Q_OBJECT
public:
    explicit lobby(QWidget *parent = nullptr);

signals:

public slots:
};

#endif // LOBBY_H
